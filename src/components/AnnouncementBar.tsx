import { AppUrl } from '@/constants';

export function AnnouncementBar() {
  return (
    <div className="bg-[#00a99d] bg-blue-600 text-white px-4 py-1 md:py-2 text-center justify-center z-50 text-xs sm:text-sm">
    </div>
  );
}
